#include "main.h"
#include "stdio.h"
#include "stdlib.h"

/**
* main - prints name with \n
* @argc: number of arguments to be counted
* @argv: array pointing to arguments
* Return: 0
*/

int main(int argc, char *argv[])
{
	argc = 0;

	printf("%s\n", argv[argc]);
	return (0);
}
